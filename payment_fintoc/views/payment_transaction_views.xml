<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_transaction_form" model="ir.ui.view">
        <field name="name">Fintoc Transaction Form</field>
        <field name="model">payment.transaction</field>
        <field name="inherit_id" ref="payment.payment_transaction_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_fintoc_cancel_refund"
                        type="object"
                        string="Cancel Refund in Fintoc"
                        invisible="provider_code != 'fintoc' or operation != 'refund' or state not in ('draft', 'pending') or not fintoc_refund_id"
                        confirm="Are you sure you want to cancel this pending refund in Fintoc?"/>
            </xpath>

            <xpath expr="//group[@name='transaction_details']" position="inside">
                <field name="fintoc_checkout_session_id" invisible="provider_code != 'fintoc'"/>
                <field name="fintoc_payment_intent_id" invisible="provider_code != 'fintoc'"/>
                <field name="fintoc_refund_id" invisible="provider_code != 'fintoc'"/>
                <field name="fintoc_redirect_url" invisible="provider_code != 'fintoc'"/>
            </xpath>
        </field>
    </record>

</odoo>
